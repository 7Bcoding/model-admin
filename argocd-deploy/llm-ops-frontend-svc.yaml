apiVersion: v1
kind: Service
metadata:
  name: llm-ops-frontend
  namespace: llm-ops
spec:
  internalTrafficPolicy: Cluster
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: llm-ops-frontend
  type: ClusterIP